import { RouterConfig } from '@angular/router';
import { OffersComponent } from './offers/offers.component';

export const AppRoutes: RouterConfig = [
  { index: true, component: OffersComponent }
];
